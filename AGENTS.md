## Agent skills

### Issue tracker

任务规格与开发票据保存在 `.scratch/<feature>/`。详见 `docs/agents/issue-tracker.md`。

### Triage labels

采用 Matt 默认的五个标签：`needs-triage`、`needs-info`、`ready-for-agent`、`ready-for-human`、`wontfix`。详见 `docs/agents/triage-labels.md`。

### Domain docs

采用单上下文布局；按需读取根目录 `CONTEXT.md` 与 `docs/adr/`。详见 `docs/agents/domain.md`。
