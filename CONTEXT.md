# 领域词汇

## 开发任务视图

开发规格、架构设计、任务依赖、实施状态与验证证据的只读投影。它不修改任何事实来源，也不把“已设计”“已实现”和“已交付”合并成一个状态。

## 架构影响

对系统组件、接口、数据流、信任边界、外部依赖或部署结构的改变。存在架构影响的工作必须经过架构设计门禁；普通文案、样式和局部修复可以记录原因后跳过。

## 规划架构

开发开始前批准的目标系统设计。它表达意图，不代表代码已经实现，也不代表存在可验证的源码证据。

## 恢复基线

从已有代码重建并经人工确认的当前架构。它用于已有系统缺少架构文档的情况，不得冒充从零开始的规划架构。

## 实际架构

实施后根据真实代码复核的系统结构。只有经过验证和批准，实际架构才能成为后续工作的长期项目基线。

## 架构批准修订

用户明确批准的架构事实源精确摘要。工具校验通过不等于批准；事实源变化后，原批准修订立即过期。

## 架构关系

系统元素之间的运行、数据或边界关系。架构关系解释系统如何组成，不直接决定开发任务的执行顺序。

## 任务依赖

一项开发工作开始前必须完成的其他工作。任务依赖构成执行 DAG，与架构关系分别建模。
